"""
app.py - 가계부 분석기 메인 앱 (내비게이션 컨트롤러)
=====================================================
이 파일은 Streamlit 앱의 진입점(Entry Point)입니다.
실행 방법: streamlit run app.py

- st.navigation()을 사용하여 페이지를 조건부로 표시합니다.
- 관리자 페이지는 admin 역할만 접근 가능합니다.
- 로그인되지 않은 상태에서는 로그인/회원가입 화면만 표시합니다.
"""

import streamlit as st

# 로컬 모듈 임포트
import db
import auth

# ============================================================
# 앱 기본 설정 (최초 1회만 호출)
# ============================================================
st.set_page_config(
    page_title="💰 가계부 분석기",
    page_icon="💰",
    layout="wide",
    initial_sidebar_state="expanded",
)

# 데이터베이스 초기화 (테이블 생성 + 관리자 시드)
db.init_db()

# ============================================================
# 인증 확인
# ============================================================
if not auth.is_logged_in():
    # 미로그인 → 로그인/회원가입 화면
    auth.require_login()
    # require_login 내부에서 st.stop() 호출되므로 아래 코드는 실행 안 됨

# ============================================================
# 사이드바 - 사용자 정보
# ============================================================
st.sidebar.title("💰 가계부 분석기")
auth.show_user_info()

# ============================================================
# 내비게이션 구성 (역할 기반)
# ============================================================
main_pages = [
    st.Page("pages/0_🏠_대시보드.py", title="대시보드", icon="🏠", default=True),
    st.Page("pages/1_📝_입력.py", title="거래 입력", icon="📝"),
    st.Page("pages/2_📋_내역.py", title="거래 내역", icon="📋"),
    st.Page("pages/3_📊_분석.py", title="소비 분석", icon="📊"),
    st.Page("pages/4_⚙️_설정.py", title="설정", icon="⚙️"),
]

nav_config = {"메뉴": main_pages}

if auth.get_role() == "admin":
    nav_config["관리"] = [
        st.Page("pages/5_🔧_관리자.py", title="관리자 패널", icon="🔧"),
    ]

pg = st.navigation(nav_config)
pg.run()
